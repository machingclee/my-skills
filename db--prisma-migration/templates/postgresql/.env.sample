# PostgreSQL connection string
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema={{schemaName}}
# Special characters in password MUST be percent-encoded
#   e.g. " → %22, ] → %5D, @ → %40, # → %23, space → %20
DATABASE_URL="postgresql://postgres:password@localhost:5432/{{databaseName}}?schema={{schemaName}}"
